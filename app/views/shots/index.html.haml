%h1 Liste des shots

%table
  %thead
    %tr
      %th Nom
      %th Description
      %th Note
      %th{colspan: 3}
  - @shots.each do |shot|
    %tr
      %td= shot.name
      %td= shot.description
      %td= shot.note
      %td= link_to 'Voir', shot
      %td= link_to 'Modifier', edit_shot_path(shot)
      %td= link_to 'Détruire', shot, method: :delete, data: { confirm: 'Are you sure?' }
